import type { Metadata } from "next";
import { Fraunces, Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const fraunces = Fraunces({
  subsets: ["latin"],
  variable: "--font-fraunces",
  weight: ["400", "500", "600", "700"],
  style: ["normal", "italic"],
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  weight: ["400", "500", "600", "700"],
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "St. Theresa Matriculation Higher Secondary School, Sendurai",
    template: "%s | St. Theresa Matric Hr Sec School",
  },
  description:
    "St. Theresa Matriculation Higher Secondary School, Sendurai, Tamil Nadu — nurturing character, curiosity and excellence since generations.",
  keywords: [
    "St. Theresa Matriculation Higher Secondary School",
    "Sendurai school",
    "Tamil Nadu matriculation school",
    "best school Sendurai",
  ],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${fraunces.variable} ${inter.variable}`}>
      <body className="flex min-h-screen flex-col font-sans">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
